package auto_application.models;
import static mod.Method.sayHello;
import static auto_application.models.Auto.printArray;
public class Garage {
    public static void main(String[] args) {
        int[] num = {2,5};
        sayHello();
        printArray(num);
    }
}
